import 'package:flutter/material.dart';

class BoxViewItem extends StatelessWidget {
  const BoxViewItem({super.key});

  @override
  Widget build(BuildContext context) {
    return Container();
  }
}